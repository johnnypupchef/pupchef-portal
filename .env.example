# PupOps API (Next.js app)
VITE_API_URL=http://localhost:3000

# Supabase Auth (anon key only — safe in browser). Same project as PupOps DB.
VITE_SUPABASE_URL=https://xxxx.supabase.co
VITE_SUPABASE_ANON_KEY=
